# .github/workflows/build.yml
name: Windows build

on:
  push:
    branches: [main]          # run on every push to main
  workflow_dispatch:          # add a “Run workflow” button

jobs:
  freeze:
    runs-on: windows-latest

    steps:
      # 1️⃣  Grab your repo code
      - name: "📥 Checkout"
        uses: actions/checkout@v4

      # 2️⃣  Install Python **3.11** (3.12 wheels are still sketchy)
      - name: "🐍 Set up Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3️⃣  Install requirements and PyInstaller
      - name: "📦 Install deps"
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      # 4️⃣  Build your single-file EXE
      #     shell: cmd → allows ^ line-continuations on Windows
      - name: "🧊 Build EXE"
        shell: cmd
        run: >
          pyinstaller app.py --onefile --noconsole ^
            --add-data "templates;templates" ^
            --add-data "static;static"

      # 5️⃣  Stash the EXE as a CI artifact
      - name: "📤 Upload artifact"
        uses: actions/upload-artifact@v4
        with:
          name: RemoteDashboard_exe
          path: dist/app.exe

      # 6️⃣  (Optional) create a GitHub Release with the EXE attached
      - name: "🚀 Release"
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: "Build ${{ github.run_number }}"
          files: dist/app.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
